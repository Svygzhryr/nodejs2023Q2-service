FROM postgres:16.2-alpine

ENV POSTGRES_USER ${DATABASE_USER}
ENV POSTGRES_PASSWORD ${DATABASE_PASS}
ENV POSTGRES_HOST ${DATABASE_HOST}
ENV POSTGRES_PORT ${DATABASE_PORT}
ENV POSTGRES_DB ${DATABASE_DB}